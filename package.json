{
  "name": "jhub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "echo \"Run from client or server folder\"",
    "install:all": "npm install --prefix client && npm install --prefix server"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
